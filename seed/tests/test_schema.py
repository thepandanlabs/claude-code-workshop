# TODO: Schema validation test.
# Confirm that extract() returns a dict with all required fields
# from PRD.md extracted schema: date, vendor, category, amount, currency, confidence
# Uses a recorded fixture — never calls the real Claude API.
