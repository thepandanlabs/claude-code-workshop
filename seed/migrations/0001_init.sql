-- TODO: SQLite schema for the receipts ledger
-- Primary key: sha256 hash of source file bytes (enables deduplication)
-- Required fields from PRD.md extracted schema:
--   id (TEXT, PRIMARY KEY) — sha256 of source file
--   date (TEXT) — YYYY-MM-DD
--   vendor (TEXT)
--   category (TEXT) — groceries|dining|transport|utilities|office|other
--   amount (REAL)
--   currency (TEXT) — SAR|USD|EUR|GBP|other
--   confidence (REAL) — 0.0 to 1.0
--   source_file (TEXT)
--   created_at (TEXT) — ISO 8601 timestamp
